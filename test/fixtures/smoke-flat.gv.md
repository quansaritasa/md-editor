Flat document

No h2 or h3 anywhere, on purpose: the outline has nothing to list, so it shows
the empty placeholder — the right panel stays open so the layout does not jump
between files with and without headings.
