Scratch pad

No headings on purpose. The outline has nothing to list, so the right panel
collapses and the document gets the full width. Handy for quick notes.

Ideas worth keeping:
- Per-folder theme memory
- Export the current document to PDF
- A compact tree mode for very deep trees
