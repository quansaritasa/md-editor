{
  "name": "md-editor",
  "version": "0.1.0",
  "description": "Markdown viewer + editor core — parse, transform, theme. Framework-free, host-agnostic. Not published to npm; consumed as a git dependency.",
  "license": "MIT",
  "author": "Quan Dao",
  "main": "dist/md-editor.js",
  "files": [
    "dist",
    "src",
    "css",
    "themes",
    "tools"
  ],
  "scripts": {
    "build": "node scripts/build.js",
    "prepare": "node scripts/build.js",
    "test": "node --test test/"
  },
  "peerDependencies": {
    "@highlightjs/cdn-assets": "^11.9.0",
    "marked": "^12.0.0",
    "mermaid": "^11.0.0"
  },
  "peerDependenciesMeta": {
    "@highlightjs/cdn-assets": { "optional": true },
    "mermaid": { "optional": true }
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
